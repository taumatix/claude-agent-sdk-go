# Audit Report — 2026-03-31

## Scope

- Files reviewed: 28 Go source files across 8 packages
- Guidelines loaded: `CLAUDE.md` (architectural principles, file layout, module conventions), `go_rule.md` (build tools, preferred libraries), `go_test_rule.md` (testing patterns), `docs/architecture.md` (concurrency model, layering), `docs/api.md` (public API contracts)

## Findings

### Critical (must fix)

| # | File | Line | Expert(s) | Finding | Suggested fix |
|---|------|------|-----------|---------|---------------|
| 1 | `domains/agent/session_manager.go` | 264, 288 | SEC, SWE, QA | `_ = sm.transport.Send(...)` in `handleControlRequest` silently discards send errors for both `can_use_tool` and `hook_callback` responses. When the send fails the CLI is left waiting indefinitely with no response. | Log or propagate send errors; at minimum send a control error response via `BuildControlErrorResponse` to unblock the CLI. |
| 2 | `shared/jsonlines/` | — | QA | `shared/jsonlines` package (Reader + Writer) has no test files. This is the lowest I/O layer; concurrent-write safety, 10 MB buffer handling, and EOF behaviour are entirely untested. | Add `shared/jsonlines/jsonlines_test.go` covering ReadLine, concurrent WriteLine calls, and large-line handling. |
| 3 | `domains/sessions/filesystem.go` | — | QA | `FilesystemStore` has no unit tests. `sanitizePath`, `findSessionFile`, `ForkSession`, `WriteSessionMeta`, and `ReadSession` are exercised only by integration against the real filesystem. | Add `domains/sessions/filesystem_test.go` with a temp-dir–based store covering the main operations. |

### Warnings (should address)

| # | File | Line | Expert(s) | Finding | Suggested fix |
|---|------|------|-----------|---------|---------------|
| 5 | `domains/messages/types.go` | 77 | SWE, UX | `RateLimitMessage.RateLimitInfo` is typed as `protocol.RateLimitInfo`, leaking an internal wire-format package into the public `messages` API. Consumers must import the `protocol` package to type-assert or switch on this field. | Define a `RateLimitInfo` struct in the `messages` package and copy the fields during `FromWire` conversion, removing the `protocol` import from `messages/types.go`. |
| 6 | `domains/agent/session_manager.go` | 309 | SWE | `sendControlRequest` wraps a cancelled channel receive in `&sdkerrors.InitializeError{}`. This error type is semantically wrong for non-initialize control requests (e.g., `SetPermissionMode`, `SetModel`). | Introduce a generic `ControlRequestCancelledError` or use `fmt.Errorf("control request cancelled")` here. |
| 7 | `domains/agent/options.go` | 113–117 | SWE, QA, UX | `BuildCLIArgs` always emits `--system-prompt ""` when neither `SystemPrompt` nor `AppendSystemPrompt` is set. This may silently override the user's configured default system prompt in the CLI, which is surprising behaviour for a zero-value `Options`. | Guard the `--system-prompt` emission behind `opts.SystemPrompt != ""`, or document clearly that empty string clears the default prompt. |
| 8 | `domains/sessions/filesystem.go` | 101 | SEC | Session metadata files (`.meta.json`) are written with `0o644` (world-readable). These files can contain session titles, first prompts, git branch names, and working directories. | Change to `0o600` so only the owning user can read session metadata. |
| 9 | `domains/agent/options.go` | 222–229 | SEC | `ExtraArgs` injects arbitrary CLI flag names without validation. A key containing spaces or `=` would produce a malformed argument string. | Validate keys in `BuildCLIArgs`: reject or sanitize keys containing spaces, `=`, or leading `-`. |
| 10 | `shared/reqid/` | — | QA | `shared/reqid` has no tests. The uniqueness guarantee (counter + 4-byte random suffix) and ID format are not exercised. | Add `shared/reqid/reqid_test.go` checking format, uniqueness across goroutines, and monotonic increment. |
| 11 | `domains/errors/` | — | QA | `domains/errors` has no tests. Error message formatting, `Unwrap` on `CLIJSONDecodeError`, and type-assertion behaviour are untested. | Add `domains/errors/errors_test.go` covering each type's `Error()` string and `CLIJSONDecodeError.Unwrap()`. |
| 12 | `domains/agent/client_test.go` | 562 | QA | `TestQuery_Interrupt` uses `time.Sleep(10 * time.Millisecond)` to wait for goroutine processing. This is a latent flaky-test risk on slow CI machines. | Replace with a channel synchronisation: let the FakeTransport's sendFunc signal when an interrupt message is received. |
| 13 | `domains/agent/session_manager.go` | — | QA | No test for `control_cancel_request` handling (`cancelPendingRequest`). The path that closes a pending channel when the CLI cancels a request is not exercised. | Add a test that injects a `control_cancel_request` wire message and verifies the pending channel is unblocked. |

### Info (consider)

| # | File | Line | Expert(s) | Finding | Suggested fix |
|---|------|------|-----------|---------|---------------|
| 14 | `domains/transport/subprocess/subprocess.go` | 106–110 | SWE | `Transport.Send` acquires `t.mu` (the transport-level mutex), but the underlying `jsonlines.Writer.WriteLine` already holds its own internal mutex. The outer lock is redundant and creates unnecessary serialisation without protecting any additional shared state. | Remove `t.mu` from `Transport.Send`; the writer's own mutex is sufficient. If `t.mu` protects the `stdin` field (for close coordination), document that explicitly. |
| 15 | `domains/agent/callbacks.go` | 28 | UX | `HookHandler` return type uses `map[string]interface{}` rather than the idiomatic Go 1.18+ `map[string]any`. | Change to `map[string]any` for consistency with modern Go style. |
| 16 | `domains/agent/options.go` | 99–104 | UX | `DefaultOptions()` leaves `PermissionMode` as `""` (empty string). The zero value is not `PermissionModeDefault`. This is a minor surprise: calling `DefaultOptions()` does not produce the "default" permission mode constant. | Either set `PermissionMode: PermissionModeDefault` in `DefaultOptions()`, or document that `""` and `"default"` are equivalent for the CLI. |
| 17 | `domains/sessions/filesystem.go` | 71–73 | SWE | The 10 MB scanner buffer size is a magic number duplicated from `shared/jsonlines`. | Extract a shared constant (e.g., `const maxScannerBufSize = 10 * 1024 * 1024`) or reference it from `jsonlines`. |
| 18 | `domains/transport/subprocess/subprocess.go` | 222–231 | SWE | Version comparison logic uses three nested conditions. The logic is correct but brittle and hard to extend for semver. | Consider a simple `semver.Compare` helper or a tuple comparison function. |

## Compliance status

NEEDS WORK (warnings remain; all criticals fixed)

## Fix log

| Finding # | Description | Status | Commit SHA |
|-----------|-------------|--------|------------|
| 1 | Silent send/hook errors in `handleControlRequest` | fixed | 56c2725 |
| 2 | No tests for `shared/jsonlines` | fixed | 09aeca5 |
| 3 | No tests for `FilesystemStore` | fixed | 09aeca5 |
